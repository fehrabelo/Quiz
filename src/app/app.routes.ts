import { Routes } from '@angular/router';
import { CardWrapperComponent } from './layout/quiz/card-wrapper.component';

export const routes: Routes = [
      {
    path: '',
    component: CardWrapperComponent,
  },
];
